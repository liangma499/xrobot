syntax = "proto3";

option go_package = "./pb";

package pb;

service Business {
  // 拉取用户佣金
  rpc Business(BusinessArgs) returns (BusinessReply) {}
  
}

message BusinessArgs {
   int64 UID = 1; //用户UID
}

message BusinessReply {
  int64 UID = 1; //用户UID
}